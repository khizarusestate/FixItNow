import fs from "node:fs";

const read = (p) => fs.readFileSync(p, "utf8");
const write = (p, s) => fs.writeFileSync(p, s, "utf8");
const replaceOnce = (s, a, b, label) => {
  if (!s.includes(a)) throw new Error(`Patch anchor not found: ${label}`);
  return s.replace(a, b);
};
const replaceRegex = (s, re, b, label) => {
  if (!re.test(s)) throw new Error(`Patch regex not found: ${label}`);
  return s.replace(re, b);
};

let messenger = read("src/Components/Messenger.jsx");
messenger = replaceOnce(messenger, 'import { messengerService } from "../services/messenger.js";\n', 'import { messengerService } from "../services/messenger.js";\nimport { PhoneCall } from "lucide-react";\nimport VoiceCallPanel from "./VoiceCallPanel.jsx";\n', "messenger imports");
messenger = replaceOnce(messenger, '  // Initial load only. New messages are delivered through the existing\n  // AuthContext Socket.IO connection via fixitnow-notification-new.\n  useEffect(() => {\n    loadConversations();\n  }, [loadConversations]);\n', '  // Initial load only. New messages are delivered through the existing\n  // AuthContext Socket.IO connection via fixitnow-notification-new.\n  useEffect(() => {\n    loadConversations();\n  }, [loadConversations]);\n\n  useEffect(() => {\n    const openBooking = (event) => {\n      const bookingId = event.detail?.bookingId;\n      if (!bookingId) return;\n      setOpen(true);\n      setSelectedBookingId(String(bookingId));\n      loadMessages(String(bookingId));\n    };\n    window.addEventListener("fixitnow-open-messenger", openBooking);\n    return () => window.removeEventListener("fixitnow-open-messenger", openBooking);\n  }, [loadMessages]);\n', "booking opener");
messenger = replaceRegex(messenger, /\n\s*<button\n\s*type="button"\n\s*onClick=\{\(\) => setOpen\(true\)\}\n\s*className="fixed bottom-5 right-5[\s\S]*?aria-label="Open messages"[\s\S]*?</button>/, "", "floating messages launcher");
messenger = replaceOnce(messenger, '                    <div className="min-w-0">\n                      <div className="truncate font-bold text-slate-900">{booking?.participant?.name || (user.type === "customer" ? "Worker" : "Customer")}</div>\n                      <div className="truncate text-xs text-slate-500">{booking?.serviceTitle || "Booking"} · {booking?.status || ""}</div>\n                    </div>\n', '                    <div className="min-w-0 flex-1">\n                      <div className="truncate font-bold text-slate-900">{booking?.participant?.name || (user.type === "customer" ? "Worker" : "Customer")}</div>\n                      <div className="truncate text-xs text-slate-500">{booking?.serviceTitle || "Booking"} · {booking?.status || ""}</div>\n                    </div>\n                    {booking?.participant?.id && booking?.status !== "completed" && (\n                      <button\n                        type="button"\n                        onClick={() => window.dispatchEvent(new CustomEvent("fixitnow-start-voice-call", { detail: { bookingId: String(booking.id), targetUserId: String(booking.participant.id), participantName: booking.participant.name || (user.type === "customer" ? "Worker" : "Customer") } }))}\n                        className="inline-flex shrink-0 items-center gap-1.5 rounded-xl border border-emerald-200 bg-emerald-50 px-3 py-2 text-xs font-bold text-emerald-700 hover:bg-emerald-100"\n                        title="Start voice call"\n                      >\n                        <PhoneCall size={15} />\n                        Call\n                      </button>\n                    )}\n', "call button");
messenger = replaceOnce(messenger, '      )}\n    </>\n  );\n}\n', '      )}\n      <VoiceCallPanel />\n    </>\n  );\n}\n', "voice panel mount");
write("src/Components/Messenger.jsx", messenger);

let myBookings = read("src/Components/MyBookings.jsx");
myBookings = replaceOnce(myBookings, '  Radio,\n} from "lucide-react";', '  Radio,\n  MessageCircle,\n} from "lucide-react";', "customer chat icon");
myBookings = replaceOnce(myBookings, '                          {b.worker && ["on-the-way", "in-progress"].includes(b.status) && (', '                          {b.worker && ["assigned", "worker-assigned", "on-the-way", "in-progress"].includes(b.status) && (\n                            <button\n                              type="button"\n                              onClick={(event) => {\n                                event.stopPropagation();\n                                window.dispatchEvent(new CustomEvent("fixitnow-open-messenger", { detail: { bookingId: String(b.id) } }));\n                              }}\n                              className="inline-flex items-center gap-1.5 rounded-lg bg-orange-500 px-3 py-2 text-xs font-bold text-white shadow-sm transition-colors hover:bg-orange-600"\n                            >\n                              <MessageCircle size={14} />\n                              Chat with Worker\n                            </button>\n                          )}\n                          {b.worker && ["on-the-way", "in-progress"].includes(b.status) && (', "customer booking chat button");
write("src/Components/MyBookings.jsx", myBookings);

let workerDashboard = read("src/Components/WorkerDashboard.jsx");
workerDashboard = replaceRegex(workerDashboard, /from "lucide-react";/, 'from "lucide-react";', "worker lucide import");
if (!workerDashboard.includes("MessageCircle")) {
  workerDashboard = replaceOnce(workerDashboard, 'from "lucide-react";', '  MessageCircle,\n} from "lucide-react";', "worker chat icon import");
}
workerDashboard = replaceRegex(workerDashboard, /(\n\s*)\{waitingCustomer && \(/, '$1{chatEligible && (\n                                      <button\n                                        type="button"\n                                        onClick={(event) => {\n                                          event.stopPropagation();\n                                          window.dispatchEvent(new CustomEvent("fixitnow-open-messenger", { detail: { bookingId: String(job.id) } }));\n                                        }}\n                                        className="inline-flex w-full items-center justify-center gap-2 rounded-lg bg-orange-500 px-4 py-2 text-sm font-bold text-white shadow-sm transition-colors hover:bg-orange-600"\n                                      >\n                                        <MessageCircle size={16} />\n                                        Chat with Customer\n                                      </button>\n                                    )}$1{waitingCustomer && (', "worker booking chat button");
const waitingDecl = '                            const waitingCustomer =\n                              !isGuestJob &&\n                              job.workerMarkedDone &&\n                              !job.customerMarkedDone &&\n                              job.status !== "completed";\n';
if (workerDashboard.includes(waitingDecl) && !workerDashboard.includes("const chatEligible")) {
  workerDashboard = replaceOnce(workerDashboard, waitingDecl, waitingDecl + '                            const chatEligible = !isClaimPending && ["assigned", "worker-assigned", "on-the-way", "in-progress"].includes(job.status);\n', "worker chat eligibility");
}
write("src/Components/WorkerDashboard.jsx", workerDashboard);

let auth = read("src/context/AuthContext.jsx");
const socketAnchor = '    socket.on("disconnect", () => {\n      // Disconnected from server\n    });\n\n    return () => {\n      socket.close();\n    };';
const voiceBridge = '    const forwardVoiceSignal = (eventName) => (data) => {\n      window.dispatchEvent(new CustomEvent(eventName, { detail: data }));\n    };\n    socket.on("voice-call-incoming", forwardVoiceSignal("fixitnow-voice-call-incoming"));\n    socket.on("voice-call-signal", forwardVoiceSignal("fixitnow-voice-call-signal"));\n    socket.on("voice-call-ended", forwardVoiceSignal("fixitnow-voice-call-ended"));\n    socket.on("voice-call-error", forwardVoiceSignal("fixitnow-voice-call-error"));\n\n    const sendVoiceStart = (event) => socket.emit("voice-call-start", event.detail || {});\n    const sendVoiceSignal = (event) => socket.emit("voice-call-signal", event.detail || {});\n    const sendVoiceEnd = (event) => socket.emit("voice-call-end", event.detail || {});\n    window.addEventListener("fixitnow-voice-call-start-send", sendVoiceStart);\n    window.addEventListener("fixitnow-voice-call-signal-send", sendVoiceSignal);\n    window.addEventListener("fixitnow-voice-call-end-send", sendVoiceEnd);\n\n' + socketAnchor.replace('    return () => {\n      socket.close();\n    };', '    return () => {\n      window.removeEventListener("fixitnow-voice-call-start-send", sendVoiceStart);\n      window.removeEventListener("fixitnow-voice-call-signal-send", sendVoiceSignal);\n      window.removeEventListener("fixitnow-voice-call-end-send", sendVoiceEnd);\n      socket.close();\n    };');
if (!auth.includes("voice-call-incoming")) auth = replaceOnce(auth, socketAnchor, voiceBridge, "voice socket bridge");
write("src/context/AuthContext.jsx", auth);

console.log("Booking messenger frontend upgrade applied successfully.");
